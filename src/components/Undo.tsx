import React from "react";
import IconButton from "@mui/material/IconButton";
import UndoIcon from '@mui/icons-material/Undo';

const Undo: React.VFC = () => (
  <IconButton aria-label="undo">
    <UndoIcon />
  </IconButton>
)

export default Undo;
